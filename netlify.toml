[build]
  command   = "npm install && npm run build"
  publish   = "dist"

[[edge_functions]]
  function = "claude"
  path     = "/api/claude"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
